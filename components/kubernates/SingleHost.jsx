import React from 'react'

function SingleHost() {
  return (
    <div className=''>
      <p>As container are hosted on single OS/ Hyperwiser software, if os is lost. We will lose all the container</p>
      <p>Due to memory constraint sometime, their is the possiblity that container is dead.</p>
    </div>
  )
}

export default SingleHost