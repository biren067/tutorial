import React from 'react'

function AutoScaling() {
  return (
    <div>
        <p>Their is load balancing.</p>
        <p>if on some occassion increase the load. Their is not way to increase the container.</p>
        <p>DevOps has to do it manually.</p>
    </div>
  )
}

export default AutoScaling